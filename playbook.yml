# vim: set filetype=yaml.ansible:
---
- name: Install tools
  hosts:
    - localhost
  tasks:
    - name: Install packages
      become: true
      ansible.builtin.apt:
        name:
          - apt-file
          - btop
          - htop
          - locate
          - lsof
          - nano
          - vim
        state: present
        update_cache: true

- name: Setup SSH Prison
  become: true
  hosts:
    - localhost
  roles:
    - geerlingguy.ssh-chroot-jail
  vars_files:
    - vars/prison.yml
